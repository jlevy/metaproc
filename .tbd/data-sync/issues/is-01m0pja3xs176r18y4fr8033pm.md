---
type: is
id: is-01m0pja3xs176r18y4fr8033pm
title: Trim agent-toolchain-bootstrap doc to point at the upstream tbd guideline
kind: task
status: closed
priority: 3
version: 3
labels: []
dependencies: []
hold: null
hold_until: null
created_at: 2026-08-23T05:43:18.713Z
updated_at: 2026-08-23T16:23:18.998Z
closed_at: 2026-08-23T16:23:18.998Z
close_reason: "Done in PR #19 (commit 9dec042). docs/agent-toolchain-bootstrap.md trimmed from 90 to 71 lines: the general case (fit test, alternatives table, portable properties, traps) is now one pointer to `tbd guidelines agent-session-bootstrap`, which get-tbd 0.8.0 ships and this repo loads. What remains is repo-specific: the pin table with each pin's canonical file, why Node is pinned rather than tracked here (npm 12 falls outside the engines range engine-strict enforces), the wiring and its ordering, what check_supply_chain.py fails on, and the pin-bump procedure. All three referring lines updated (README doc-map row, SUPPLY-CHAIN-SECURITY.md, environment-bootstrap runbook), since each described the doc as the record of the reusable pattern. make verify green including local Markdown link checks; CI green."
resolution: null
duplicate_of: null
---
`docs/agent-toolchain-bootstrap.md` was written before the pattern existed upstream. It
has since been generalized into a tbd guideline, `agent-session-bootstrap`, merged in
jlevy/tbd PR #248 (`c4dd9f38`). Once that ships in a released `get-tbd` and this repo
adopts that release, most of the metaproc doc becomes a duplicate of the guideline.

Trim it to the parts that are actually Metaproc-specific — which pins this repo uses,
where the script lives, how `check_supply_chain.py` guards it — and point at
`tbd guidelines agent-session-bootstrap` for the general pattern, the fit test, the
alternatives, and the PATH and pin-drift traps.

BLOCKED until: `agent-session-bootstrap` appears in a released `get-tbd` version and
that version is adopted here under the supply-chain policy (first-party exemption
applies, but the version change still needs a review and an updated rationale in
SUPPLY-CHAIN-SECURITY.md). Verify with `tbd guidelines --list` after upgrading. Do not
trim before then: the doc would point at a guideline this repo cannot load.

Also update the README documentation-map row and the SUPPLY-CHAIN-SECURITY.md link that
reference the doc, and re-run `make verify` (local Markdown link checking is part of it).

## Notes

UNBLOCKED as of get-tbd 0.8.0, adopted on branch claude/docs-review-ci-fixes-7re7um
(PR #19). Both halves of the blocking condition are satisfied: the guideline ships in a
released version, and that version is adopted here with the rationale the supply-chain
policy requires recorded in SUPPLY-CHAIN-SECURITY.md.

Verified in this repo: `tbd guidelines --list` shows `agent-session-bootstrap`, and
`.tbd/config.yml` maps `guidelines/agent-session-bootstrap.md` in docs_cache. Note that
`tbd guidelines --list` run against an OLD docs_cache config will NOT show it, which is
misleading: the list reflects the repo's cached doc set, not the installed CLI's bundled
set. Run `tbd setup --auto` first, then check.

PR #19 did the small piece only: the doc's closing line no longer calls the pattern "a
candidate to graduate into a shared guideline" and points at the guideline instead. The
actual trim is still open and is this bead's work -- deciding how much of the
Metaproc-specific record to keep, plus the README documentation-map row and the
SUPPLY-CHAIN-SECURITY.md link.
